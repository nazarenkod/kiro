<!--
  Заготовка MR-шаблону для згенерованих gen-test тестів (рішення 13.5).
  УСТАНОВКА (одноразово, людина): скопіювати цей файл у цільовий репо як
      .gitlab/merge_request_templates/gen-test.md
  Після цього він — людське джерело правди; агент mr-composer його ЧИТАЄ і
  ЗАПОВНЮЄ, не переписує. Секції не видаляти й не переставляти.
  Блок доказів між маркерами регенерується агентом; решта — людське.
-->

## Що це за MR

Згенеровані UI-автотести (skill `gen-test`) за перевірками крит-функціоналу з
TestManager. Кожен тест: компільований, зелений на потрібних платформах,
пройшов негативний контроль.

<!-- Заповнює mr-composer: короткий опис набору. -->

## Перелік тестів

<!-- Заповнює mr-composer з mr_stats.py: додані/змінені тести + TMId. -->

| TMId | Тест | Файл | Статус |
|---|---|---|---|

## Докази прогонів

<!-- gen-test:evidence -->
<!-- Цей блок повністю регенерується агентом mr-composer з metrics/runs.jsonl
     + traceability-карти з plan-<checkId>.md. Не редагувати вручну між
     маркерами. Для кожного TMId: платформи, negative_control, debug_iterations,
     scroll_fix, reveal_used, + карта step/expected → ассерт (покриття ТМ). -->
<!-- /gen-test:evidence -->

## Зміни у стабах / тест-даних

<!-- Заповнює mr-composer: нові JsonFilePath-маппінги, якщо є. -->

## Прогалини та рішення

<!-- Заповнює mr-composer: як закриті gaps (відповіді людини з плану). -->

## Чек-лист рев'ювера

<!-- Агент НЕ відмічає ці пункти — це відповідальність рев'ювера. -->

- [ ] Тест відповідає `conventions.md` (DSL, PbComponents, структура).
- [ ] Контракт стабів (`request`/`response`) відповідає TestManager.
- [ ] Негативний контроль справді ламає **цільовий** ассерт.
- [ ] Навігація/user/card адекватні передумовам перевірки.
- [ ] Тест лежить у правильній feature-теці.
